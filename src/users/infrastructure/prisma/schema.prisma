generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("USERS_DATABASE_URL")
}

model UserRecord {
  id      Int    @id @default(autoincrement())
  name    String
  surname String
  email   String @unique
  address String
  phone   String
}
